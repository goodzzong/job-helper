mixin portfolio(item = {})
	.portfolio
		a(href=routes.detailPortFolio(item.id))
			img.portfolio__thumbnail(src=item.imageFile)
			.portfolio__info
				.portfolio__title=item.title
				h6.portfolio__views=item.views