extends layouts/main
include mixins/portfolio

block content
	.main-title 우리들의 작품
	.portfolio-list
		each item in portfolios
			
			+portfolio({
					id: item.id,
					title:item.title,
					views:item.createdAt,
					imageFile:item.imageFile
			})

			+portfolio({
					id: item.id,
					title:item.title,
					views:item.createdAt,
					imageFile:item.imageFile
			})

			+portfolio({
					id: item.id,
					title:item.title,
					views:item.createdAt,
					imageFile:item.imageFile
			})

			+portfolio({
					id: item.id,
					title:item.title,
					views:item.createdAt,
					imageFile:item.imageFile
			})

			+portfolio({
					id: item.id,
					title:item.title,
					views:item.createdAt,
					imageFile:item.imageFile
			})

			+portfolio({
					id: item.id,
					title:item.title,
					views:item.createdAt,
					imageFile:item.imageFile
			})

			+portfolio({
					id: item.id,
					title:item.title,
					views:item.createdAt,
					imageFile:item.imageFile
			})

			+portfolio({
					id: item.id,
					title:item.title,
					views:item.createdAt,
					imageFile:item.imageFile
			})