extends layouts/main
include mixins/portfolio

block content
	h1 Home Goodzzong
		.portfolio__list
			each item in portfolios
				+portfolio({
						id: item.id,
						title:item.title,
						views:item.views,
						imageFile:item.imageFile
				})